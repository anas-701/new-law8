import { UnsubscribeService } from './unsubscribe.service';

describe('UnsubscribeService', () => {
  const service: UnsubscribeService = new UnsubscribeService();

  it('should create', () => {
    expect(service).toBeTruthy();
  });
});
